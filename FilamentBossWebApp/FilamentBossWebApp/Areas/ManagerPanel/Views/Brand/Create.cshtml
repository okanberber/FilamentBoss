@model FilamentBossWebApp.Models.Brand
@{
    ViewBag.Title = "Marka Ekle";
    Layout = "~/Areas/ManagerPanel/Views/Shared/_LayoutPage1.cshtml";
}
@section styles
{
    <link href="~/Areas/ManagerPanel/Assets/css/FormStyle.css" rel="stylesheet" />
}
<div class="pagetitle">
    <h2>Marka Ekle</h2>
</div>
<div class="formContainer">
    @using (Html.BeginForm())
    {
        if (ViewBag.mesaj != null)
        {
            <label class="validationmesage">@ViewBag.mesaj</label>
        }
<div class="row">
    <label>Isim</label>
    @Html.EditorFor(x => x.Name, new { htmlAttributes = new { @class = "textbox" } })
    @Html.HiddenFor(x => x.Supplier_ID, new { @Value = ViewBag.Supplier_ID })
    @Html.ValidationMessageFor(x => x.Name, "", new { @class = "validationmesage" })
</div>
        <div class="row">
            @Html.EditorFor(x => x.IsActive, new { htmlAttributes = new { @id = "isActive" } })
            <label for="isActive"> Aktif Marka</label>
        </div>
        <div class="row">
            <button type="submit" class="formbutton">Marka Ekle</button>
        </div>
    }
</div>

